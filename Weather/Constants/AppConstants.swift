//
//  AppConstants.swift
//  Weather
//
//  Created by Ashish Verma on 30/05/21.
//

import Foundation

enum AlertTitles {
    static let addPlaceToListTitle = "Add place to list?"
}

enum AlertMessages {
    static let addPlaceToListMessage = "This place will be listed on the home screen once you add it."
}

enum ButtonTitles {
    static let add = "Add"
    static let cancel = "Cancel"
}

enum InforMessages {
    static let noPlacesToShow = """
                                No places to show yet.\n
                                Long press on the map to add a place in the list.
                                """
}
